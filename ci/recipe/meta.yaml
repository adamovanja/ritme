{% set data = load_setup_py_data() %}
{% set version = data.get('version') or 'placehold' %}

package:
  name: ritme
  version: {{ version }}

source:
  path: ../..

build:
  noarch: python
  script: make install

requirements:
  host:
    - python
    - setuptools
    - pip

  run:
    - python
    - grpcio==1.65.5
    - importlib-metadata
    - qiime2::qiime2
    - qiime2::q2-feature-table
    - qiime2::q2-phylogeny
    - lightning
    - mlflow
    - numpy
    - optuna
    # fix to avoid clobberwarning when conda building
    - opencensus==0.11.3
    - opencensus-context==0.1.3
    - packaging
    - pandas
    - pip
    - pytorch
    - python-dotenv
    - py-spy
    - py-xgboost
    - ray-default
    - ray-tune
    - scipy
    - scikit-learn
    - scikit-bio
    - setuptools
    - torchmetrics
    - torchvision
    - typer
    - wandb
    - zipp

channels:
  - qiime2
  - conda-forge
  - bioconda
  - pytorch
  - defaults

dependencies:
  - anaconda-client
  - conda-build

test:
  requires:
    - parameterized
    - coverage
    - pytest
    - pytest-cov
  imports:
    - ritme
  commands:
    - pytest -v --cov ritme --pyargs ritme

about:
  home: https://github.com/adamovanja/ritme
  license: BSD-3-Clause
  license_family: BSD
