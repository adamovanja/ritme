coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      default:
        # Target total project coverage
        target: 80%
        threshold: 2%
    patch:
      default:
        # Target coverage for changes in PRs
        target: 80%
        threshold: 1%
